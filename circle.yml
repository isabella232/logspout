machine:
  services:
    - docker

dependencies:
  pre:
    - make circleci
  override:
    - make build

test:
  override:
    - /bin/true

deployment:
  release:
    branch: /release.*/
    commands:
      - make release
